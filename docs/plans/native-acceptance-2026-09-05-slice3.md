# Native acceptance — 2026-09-05, Slice 3 milestones

This ledger continues the [Slice 2 gap list](native-acceptance-2026-09-05.md). Android sign-in transport can be deployed independently of native renderer qualification. Slice 2 exit, supported native distribution, full Applet isolation, eviction and performance budgets are not yet accepted.

## A — production Android sign-in

The deployment switch is account-wide, not per-User: `NATIVE_SLICE_2_AUTH=android`. Better Auth remains the identity source; the User DO owns one-use session admission/revocation. Browser and native admission share `accountIsAdmitted`: a closed signup gate cannot be bypassed by the native session's first User write. Failure to record issuance returns no bearer. Exact PKCE returns, replay refusal and compatibility binding remain mandatory. No new authority, setting, grant, secret, Computer call or kernel behavior is introduced. [Delivery](../architecture/delivery.md#native-sign-in-rollout) records activation and rollback.

Physical Pixel 9a (Android 17, `CP2A.260805.005`) was connected and unlocked. The actual installed APK was inventoried and pulled. Its SHA-256 signer, the candidate's signer and the existing keystore all match `61:E6:47:9F:9C:57:55:15:4C:1F:93:9C:DE:48:E8:A7:57:EF:F3:13:6E:54:ED:1D:DA:5F:61:E7:8B:3C:1E:37`. Release-mode **1.1.0+3** replaced **1.1.0+2** with `adb install -r` after a fresh greater-version check. No uninstall or key replacement occurred. Authenticated continuity awaits A's production release.

The Apple Silicon macOS release compiled with the exact pinned SDK and ran locally with ad-hoc signing. Its real window was 800 × 632. Build/cache outputs remained inside this worktree. Xcode's nested manifest sandbox was disabled for this build inside the existing execution sandbox; no application sandbox entitlement was changed. Launch/window capture required the execution tool's approved access to Launch Services/WindowServer. The sole installed provisioning profile is for `com.nookk.app` on iOS, not FrockBot/macOS; the Apple association is published for later use, but macOS auth remains closed pending a matching signed target. No iOS work or store submission was attempted.

### Design review

The native theme derives from `plugin-ui-theme/src/client/theme.css` and the shell styles: bundled Manrope/Archivo Black with their licenses, sheep artwork already used by the app icon, pink action color, dark semantic surfaces, grouped labels, 10/14/20 px corner vocabulary, tabular small numerals and platform touch sizes. A light palette is defined and widget-tested; the current product defaults to its existing dark theme. Sign-in scrolls under large text and narrow windows, uses safe insets and live status announcements, has explicit retry/browser-handoff copy, finite skeletons and reduced-motion-aware feedback. Qualification form navigation exists only after authentication in `NATIVE_ACCEPTANCE` builds, never on sign-in.

Actual release-window captures: [Pixel sign-in](native-evidence-2026-09-05-slice3/pixel-sign-in.png), [Pixel request failure before A deployment](native-evidence-2026-09-05-slice3/pixel-sign-in-failure.png), [macOS sign-in](native-evidence-2026-09-05-slice3/macos-sign-in.png). These show the production widgets, not a mock or form fixture. Loading/browser handoff/offline/unavailable states pass at 200% text scaling in both palettes; physical screenshots of the authenticated handoff and those transient states remain part of B. No frame-rate, IME, screen-reader or interaction-latency claim follows from screenshots.

### Verification and constitutional review

- Forced dependency installation passed with worktree-local temp/cache directories. Playwright remains exactly 1.62.1; Applets and generated foundation artifacts were not edited.
- Repo-wide typecheck: all 82 packages. Bun: 4,644 passed, one existing skip. Flutter: 209 passed, analyzer clean. Formatting, UI-style, kernel-import, Computer-host-import, protocol freshness, generated-artifact and native pin/digest checks pass.
- Gateway tests cover concurrent replay, null durable issuance, signup refusal before provisioning, exact Android/macOS return gating, public non-redirecting association responses, disabled-route refusal without application loading, browser auth preservation, callback ambiguity, PKCE mismatches/expiry and unsafe Google redirect origins. Existing production User-DO workerd tests cover persistent issuance, revocation and cross-User refusal.
- Production integration suite after the final shared account-policy extraction: 128 tests passed across 51 files. Device and full release-budget gates stay below rather than being inferred from those tests.

| Constitutional rule family                          | Review of this milestone                                                                                                                                                                    |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Product intent, constitutional gate, feature rule   | Beyond-parity renderer/auth work under ADR 0039 and Slice 2/3 plan. Android transport activation is bounded; renderer/catalog/support promotion is not claimed. No amendment.               |
| Authorities, durable effects, migrations            | Existing User session schema and atomic issuance/revocation preserved. Refusal before reply on failed admission; callback replay cannot create a second session. No stored shape changes.   |
| One production path, minimal kernel, explicit seams | Same identity and account admission policy; gateway routes to existing owners. Dart remains a projection/command client. Kernel and provider adapters untouched; import checks pass.        |
| Configuration, settings surfaces, integrations      | No new product control or per-Bot configuration. Rollout is a deployment switch. Auth and its Google credentials remain backend-owned. Existing Vue/Capacitor auth tests pass.              |
| Composition, self-modification, contributions       | No executable extension, Package authority, generation, catalog advertisement, or trust-chrome seam is added. Form fixture remains build-gated. Unqualified fallback gates remain explicit. |
| Computer, Workspace, Memory                         | No Computer invocation or durable-root/Memory behavior change. Applet-with-hibernated-Computer evidence is still required in B.                                                             |
| Architecture checks, documentation, landing         | Mandatory checks and actual release builds recorded; existing plan/acceptance gaps retained. This milestone requests integration only; the orchestrator tags/releases.                      |

### A follow-up — verification hostname

PR #263 merged as `86d8f5fe`; the orchestrator deployed v0.3.32. The canonical association URL returned 200 at 13:05 AEST. On the real Pixel, Google sign-in reached the HTTPS return, but Android still reported domain-verification error 1024. Google's Digital Asset Links response reported a cached 401 from `https://bot.frockbot.com./.well-known/assetlinks.json` and a 600-second cache window. A direct probe reproduced a current **403 for the root-dot hostname versus 200 for the canonical hostname**.

[PR #267](https://github.com/timoconnellaus/frockbot/pull/267), opened by the concurrent production QA session, normalizes the fully qualified hostname at the gateway and supplies HEAD for both association files. The exact return-URI allowlist and PKCE binding remain in force. This is a gateway correction with no durable-state, authority, credential, settings or UI change. The physical verification and authenticated flow must be repeated after its release and verifier cache refresh; HTTP 200 alone does not prove Android verification.

## B — authenticated devices and budget ledger

Canonical and DNS root-dot association delivery now pass after #267. Android currently reports `approved`, changed outside this session; automatic domain verification remains unproven. The Pixel is currently unlocked; do not wait on a future locked device or bypass keyguard. macOS verified return requires a FrockBot provisioning/signing profile. All unchanged Slice 2 budgets remain unaccepted until measured against the specified fixture.

Outstanding: automatic verified sign-in; paged history, uncertain delivery and reconnect/eviction correlation; the published Todo Applet `vgpqfaCcwnPlzjYdb2mIfNcOW1YV0SkG.e1f813c4b3398e3ee947b323b9996491` with hibernated Computer; physical fallback isolation; client kill plus forced DO eviction; 10,000-Turn performance, 30 editable cold/warm launches, three five-minute runs and 20 Applet/lifecycle cycles; IME, accessibility and remaining native platform gates.

### B preparation before authenticated qualification

The dedicated `Native QA Sep 5` Bot (`native-qa-20260905`) was created through the production directory command with receipt `native-qa-create-20260905-codexnative3`, revision 14. Browser identity matches Tim's User, and the original Bots remain present. Android release **1.1.0+6** upgraded in place through greater version codes 4, 5 and 6 with the original signer. Chat now uses themed message surfaces, finite insertion motion, explicit empty/loading states, pull-to-refresh, keyboard dismissal and send/Stop haptics. Applet directory/fallback regions have explicit loading, empty and retry states. WebView inspection is enabled only in `NATIVE_ACCEPTANCE` builds for the pending physical isolation checks. The runner checks for an unlocked phone before UI/launch loops and refuses an offline or locked device promptly.

Flutter analyzer and 210 widget/contract tests passed, including all six chat states at 200% text with reduced motion. These tests and the Android/macOS release builds establish preparation only; none substitutes for the still-open authenticated, isolation and budget rows. [Actual Pixel browser-handoff state](native-evidence-2026-09-05-slice3/pixel-sign-in-handoff.png) is captured in release mode. The shared Pixel was also used by a concurrent QA session. This session resumed checks only on the dedicated Native QA Sep 5 Bot.

PR #265 repaired release secret handling. `APPLET_VIEWER_SECRET` is now present in GitHub production, and the production browser viewer-token endpoint returns 200. Its value was never read or passed into Dart.

### B device results and corrections

The Pixel's protected session is authenticated as Tim's User `vgpqfaCcwnPlzjYdb2mIfNcOW1YV0SkG`; the original directory and dedicated QA Bot are present after in-place upgrades through **1.1.0+9**. Another operator completed sign-in and changed Android's association state to `approved`. This proves current authenticated access and retained account identity, **not** this session's observation of automatic verified-link sign-in. No native bearer or browser cookie was transferred manually.

| Physical scenario                     | Observed evidence                                                                                                                                                                         | Limit                                                                                                                       |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Select Bot and send                   | Native QA Sep 5 displayed the exact requested reply. Run `nd8rPyeMZxkwta3HBpYrJ1u6Nugq7WTDa`, admitted `2026-09-05T04:35:42.252Z`, is durably `completed`, with one `send/to-user` event. | Two-Turn history; large paged-history fixture remains open.                                                                 |
| Explicit Stop                         | Run `nO2ZD-oHV2t2a54k9Yfv8BqsV306IJkac`, admitted `2026-09-05T04:45:02.604Z`, has `stopRequestedAt=2026-09-05T04:45:06.542Z` and terminal `cancelled`. Pixel displayed Stopped.           | Lost Stop acknowledgement and forced DO eviction remain open.                                                               |
| In-place replacement / client restart | Versions 7, 8 and 9 passed immediate greater-code checks and the original debug-signer assertion; QA Bot and both terminal rows reappeared.                                               | No forced owner eviction or uncertain-admission network loss in this check.                                                 |
| Deterministic form                    | Release UI accepted Native durable form test and displayed Saved after the receipt. Hostile cyclic document became a local unavailable region with Back retained.                         | Full catalog action handling stays unqualified.                                                                             |
| Real published Todo                   | Production directory returned the exact requested Applet. Native initially rejected its timestamped generation; after the schema correction it reached the bootstrap, which returned 500. | Needs deployment of the RPC correction below, then actual facet mutation/reconnect/isolation checks.                        |
| Computer                              | Read-only production projection before Applet work was idle, Ready to start, with no screenshots.                                                                                         | The Computer has not been opened or woken by this session. A hibernated live-host fixture is not established by idle alone. |

The real payload exposed two previously untested seams: `currentGenerationId` and fallback generation IDs must accept the existing timestamp-plus-digest form, and the gateway must not add `userId` to an RPC already scoped by server-created User props. Shared TypeScript/Dart fixtures now contain the published generation format. The production workerd gateway test reproduces the old 500 and checks successful bootstrap, exact User/generation/token binding, two-minute expiry, cross-User refusal, native authentication and zero Computer-host calls. Failed bootstrap reads return plain repair copy. No Applets Package or generated foundation Applet artifact was changed.

Release captures: [reply](native-evidence-2026-09-05-slice3/pixel-chat-reply.png), [explicit Stop](native-evidence-2026-09-05-slice3/pixel-chat-stopped.png), [saved deterministic form](native-evidence-2026-09-05-slice3/pixel-form-saved.png), [hostile form region](native-evidence-2026-09-05-slice3/pixel-form-unavailable.png). Device diagnostics are build-gated and report only a fixed stage, exception type and HTTP status; no URL, response, User identifier or credential is logged.

Design review: shared dark type and surfaces, pink primary actions, inset-aware composer, plain terminal and unavailable copy, retained host navigation, finite message insertion, reduced-motion widget coverage and 200% text layout were checked. These screenshots are actual release pixels. Physical TalkBack, predictive-back and full IME/latency qualification remain open; authenticated macOS screens require the matching provisioning profile and enabled verified-return target.

The final chat capture is [release +9 after restart](native-evidence-2026-09-05-slice3/pixel-chat-restored.png). Chat starts at the latest row; loading earlier pages preserves the current reading position in the regression widget test. [macOS release +8 sign-in](native-evidence-2026-09-05-slice3/macos-b-sign-in.png) confirms the local build/window; authenticated Mac captures remain blocked by signing, rather than being replaced with mocked screenshots.

### B measured budget status

[Sanitized release +8 measurements](native-evidence-2026-09-05-slice3/pixel-release8-measurements.json) record 30 cold/warm activity pairs and 20 resume cycles against the authenticated **two-Turn** QA chat. All 30 cold processes reported the Dart editable-widget counter: p95 **129.927 ms**, p99 **139.088 ms**, measured after the Dart stopwatch starts. These are not OS-to-editable launch times. Only the initial Flutter timing callback was captured from each process; its p95 build/raster values were 1.230/8.665 ms. No typing/history frame-rate acceptance follows from startup callbacks.

Base PSS was **199.28 MiB**, exceeding the unchanged **180 MiB** target. After 20 resume cycles it was **191.73 MiB**. Graphics accounted for 113.44 MiB of the base sample. The larger 10,000-Turn fixture, controlled 60 Hz, three five-minute interaction runs, hardware/IME input-to-paint, foreground idle average and Applet open/close retention are unqualified. Version +9's viewport correction has not been used to reinterpret these +8 measurements. No budget or supported-native promotion is claimed.

### B verification and constitutional review

Repo-wide typecheck passes all 82 packages, Bun passes 4,712 tests with one existing skip, production workerd integration passes 130 tests in 53 files, and Flutter passes 219 tests with a clean analyzer. Formatting, UI style, kernel/Computer-host imports, generated protocol/artifact checks and native pins pass. These checks were rerun after merging main, resolving the entrypoint annotation in favor of main’s `Event` type, and forcing dependency installation for the restored Connectors package. Root Playwright remains 1.62.1.

| Constitutional family                               | Evidence / disposition                                                                                                                                                                                                                                                                                             |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Product intent, gate, feature rule                  | This is a bounded repair and device-evidence milestone within the accepted Slice 2 prototype, beyond parity. It does not declare Slice 2 closed or promote native distribution/catalogs. Owners and remaining gates are explicit above; no constitutional amendment.                                               |
| Authorities, durable effects, cancellation          | Existing User session/form receipts and Bot admission/Stop owners remain authoritative. Real native receipts correlate to the same browser read model. UI disconnect only detaches; no automatic Stop or send on restart. Forced eviction and uncertain network loss remain acceptance gates.                      |
| One production path, minimal kernel, explicit seams | Gateway uses the same User-scoped Worker entrypoint and Applet authority. Strict RPC decoding is retained; its caller is corrected. Both generated validators accept actual durable generation identifiers. Dart holds projection, protected draft/session and UI code only; no Agent loop or provider credential. |
| Configuration, settings, integrations               | No new product configuration, grant or per-Bot override. Google and Applet signing credentials stay on the backend. The existing required base remains available when extension loading fails.                                                                                                                     |
| Composition, contributions, self-modification       | No Applets Package mutation, new executable contribution, catalog advertisement or authority expansion. Artifact, User, generation and short-lived lease are bound together. Cross-User bootstrap fails closed with plain retry copy. Full physical isolation remains a promotion gate.                            |
| Computer, Workspace, Memory                         | No Computer open/wake from the native checks. Integration asserts no Computer-host calls while acquiring an Applet lease. Workspace and Memory authority/writers are unchanged. Idle projection is not presented as proof of a hibernated live Computer.                                                           |
| Stored shapes, compatibility, trust                 | Existing persisted owners/records are unchanged. The generation wire correction handles the already-released producer shape. Compatibility remains negotiated; unsupported clients and non-native access fail before the fallback. No secret in fallback URLs, logs or Dart.                                       |
| Checks, documentation, landing                      | Real release screenshots and measured limits are retained. This PR supplies the production RPC repair needed before the real Applet can be completed; the remaining device/desktop/budget gates stay open. Integration and orchestrator-owned release remain separate.                                             |

### B follow-up — live viewer protocol

PR #275 merged and the v0.3.39 app deployment passed. That release's independent npm publishing job failed; the deployed app is available. On the unlocked Pixel, release **1.1.0+10** replaced +9 with the original id and signer after a fresh version check. The published Todo now loads in the isolated WebView, then fails its handshake. No Todo was added or changed.

The physical check exposed an Android bridge mismatch: returning `JSON.stringify(epoch)` is encoded again by Android's JavaScript bridge, while WebKit returns the string directly. The host now compares the exact navigation epoch inside JavaScript and receives a boolean. Platform-contract regression tests cover both bridges and stale/missing epochs.

A separate, confirmed gate remains in the exact requested Todo generation **2026-09-05T00:52:26.826Z:e0e3ce78fabf9eca**, artifact **e9851aa9295c2fe58a34ee9ccb56c8f8754d1e798c4febd7e56ced99c677de73**. Read-only inspection of the published bytes shows its bundled SDK has no `applet/ready`, `tokenTransport`, or `subprotocol-v1`, and puts its viewer token in the socket URL. The native bootstrap waits for the declared subprotocol handshake and never supplies a token to that page. The page must be republished through the existing Applets authoring path with the current SDK before its real native mutation/reconnect/isolation flow can pass. No credential was copied, no native-only token was downgraded, and the immutable published artifact was not edited.

Release **1.1.0+11** adds specific recovery copy when the page loaded but did not establish the handshake. The reviewed Back control and Applet attribution remain available. [Pixel loaded-page recovery](native-evidence-2026-09-05-slice3/pixel-todo-unavailable.png) and [macOS release sign-in](native-evidence-2026-09-05-slice3/macos-handshake-sign-in.png) are actual release captures. macOS authenticated viewing remains blocked by the matching provisioning profile; its screenshot does not stand in for that check. Full performance budgets and supported-native status remain unqualified.

The QA Bot now has additional activity after the original two observed Turns. The original completed reply and cancelled Stop still read back with their recorded run IDs; later activity is excluded from this session's two-Turn measurement evidence. The Computer projection remains idle, Ready to start, without a Computer call from these checks. This still does not establish a hibernated live-host fixture.

Design review: loaded-page failure has distinct, actionable copy, a retry button, readable dark contrast and retained host navigation/attribution. The bridge fix adds no authority, durable state, protocol exception, provider credential or Computer dependency. Existing origin, generation, live session and native-only lease checks remain mandatory. Repo-wide checks pass: 82 typecheck packages, 4,722 Bun tests (one skip), 132 integration tests across 54 files, 221 Flutter tests and clean analyzer; format, UI style, architecture/generated contracts and native pins pass. Slice 2 is still open.

## C1 — shared Settings and Models

Vue and Flutter now render the User owner's settings frames and use the same owner-bound configuration commands. Models offers the permanent account default, paged search and manifest-declared provider fields. The optional Custom models Package retains only Bot overrides. A provider account opens an expiring, same-User backend browser handoff; no provider secret enters Dart. [ADR 0043](../adr/0043-account-model-choice-survives-disabled-overrides.md) records the additive account-model record and bounded previous-reader projection.

After merging main through #285, all 82 packages typecheck; 4,797 Bun tests pass (one existing skip), 134 workerd integration tests pass across 55 files, and Flutter analyzer plus 247 tests pass. Five actual Chromium scenarios against the local production Worker pass: valid/refused provider authorization, profile prefill/save, account model choice with overrides disabled, and lost-save reply followed by reload and replay of the original command. Paired browser/native gateway cases use one User owner and cover owner eviction, duplicate receipt, stale revision and cross-account refusal. Transaction-failure and literal previous-released-reader tests qualify this settings seam only.

The macOS release build succeeds using the exact pinned Flutter SDK. Its test-only entrypoint talks to the production Worker harness under a separate local test User and a separate non-secret command store. It never reads the installed production session and cannot be selected by the ordinary product entrypoint. [Release offline state](native-evidence-2026-09-05-slice3/macos-settings-offline.png) is an actual local fault-proxy capture taken before the orchestrator restart, not a production outage. After restart, macOS reported screenshot access but no Accessibility access. Opening the app through Launch Services allowed a [current release Settings capture](native-evidence-2026-09-05-slice3/macos-settings-home.png), showing the real User projection and declared Image generation settings. Automated window interaction remains unavailable; Models/picker and transient-state release captures remain open. The Settings screenshot does not establish those states.

Design review: shared neutral dark cards, pink actions, grouped labels, bounded desktop width and safe insets; searchable paged model selection; specific load/refusal/uncertain-save recovery; a retained command must be checked before another change. Native widget scenarios cover 200% text in both palettes and reduced motion. Physical screen-reader, interaction frame-rate and input latency remain unqualified. The Pixel was unlocked after restart and another operator had installed versionCode 15. This session did not install its older local-design candidate; Pixel C1 captures await production deployment and an in-place higher-code release build. Production macOS verified sign-in still requires its matching provisioning profile.

The Dart import review finds Flutter UI, generated DTOs, transport, command storage and URL launching in the Settings module; no Agent loop, provider SDK or provider credential implementation. Full Slice 2 budgets, the legacy Todo republish and C2–C4 remain open. This milestone integrates the working slice under the owner's restart instruction to ship working parts with explicit evidence gaps; it does not promote qualified native distribution.

C1 CI follow-up: all four browser shards passed. Validate found a shared Workerd bootstrap still writing the removed Package account-model field; it now uses `user/set-account-model`. The full runtime suite then passed 217 tests (one skip). After merging #286, the required checks passed again: 82 typecheck packages, 4,821 Bun tests (one skip), 135 integration tests across 56 files, 247 Flutter tests with a clean analyzer, formatting, UI styles and native pins. Android release +16 built with the existing package identity; installation and physical C1 screens still await deployment.

C1 production update: PR #287 merged as `317f3b74`. The orchestrator's v0.3.47 app and marketing deployments succeeded; its separate package-publishing job failed. The production association URL still returns 200. The fresh Pixel gate then found keyguard active or unconfirmed and refused installation before changing the phone. Release +16 is built with the original package and signer but remains uninstalled: **awaiting an unlocked phone**.

## C2 — native Connectors

Native shows bounded account labels, service names and lifecycle states from the User owner. Model provider accounts stay on Models; revoked and disabled Package accounts follow the same manifest home policy as Vue. Catalog search, grants, credential entry, reconnect and revocation use the existing backend Connectors surface in the system browser. The native page refreshes on return. This milestone does not claim a separately rendered native catalog or expose a secret-entry form in Dart.

The paired gateway scenario completes a browser grant, replays its callback, evicts the User owner, reads one ready Connection from native, revokes it and checks that it disappears while another User sees none. Shared strict DTO fixtures reject credential metadata and unknown lifecycle states. Native tests cover the Settings navigation, load failure/retry, exact handoff origin, browser return refresh and 200% text in both themes with reduced motion. The browser's real Chromium grant and disconnect scenario passes against the local production Worker with the external provider stubbed.

Actual macOS release +17 captures use the final production widgets, a separate local User and the existing production Worker harness: [connected](native-evidence-2026-09-05-slice3/macos-connections-connected.png), [empty after revocation](native-evidence-2026-09-05-slice3/macos-connections-empty.png), [loading through the local fault proxy](native-evidence-2026-09-05-slice3/macos-connections-loading.png), [offline recovery](native-evidence-2026-09-05-slice3/macos-connections-offline.png). The local grant is synthetic Gmail authorization, not Tim's live Google account. No production session was copied into the local runner.

Design review: shared typography, pink primary action, neutral grouped account cards, bounded desktop width and safe insets; readable status with a specific recovery route; a finite account-entry animation disabled by reduced motion; skeleton loading and clear empty/offline copy. No raw provider error reaches Dart. Physical screen-reader and full latency/frame-rate budgets remain unqualified. The Pixel capture gate is **awaiting an unlocked phone**; production macOS verified auth still requires its matching provisioning profile. [The seam plan](native-connections.md) records the complete constitutional review and the browser handoff choice.

C2 final checks after merging #287–#289: all 82 typecheck packages, 4,825 Bun tests (one existing skip), 137 integration tests in 58 files, 255 Flutter tests with a clean analyzer, format/architecture/UI/pins checks pass. Real Chromium Gmail and Google Calendar grant/disconnect scenarios both pass against the restarted current-main Worker harness.

## C3 — inbox, unread and Bot links

Native projects shared pending notices and unread state. Explicit read/unread commands persist their identity before dispatch; repeated acknowledgement converges with Vue after Bot owner eviction. Polls do not mark a conversation read. Failed notices use #260’s backend copy. A failed Turn can have a notice without a chat unread cursor, so the paired scenario tests those states independently. The notification fan-out accepts the existing critical urgency field, fixing its previous strict-decoder refusal.

Root `?bot=` navigation checks the exact production origin/path and one valid Bot id, then resolves membership from the authenticated live directory. Android declares the verified root path alongside the exact auth return. Physical root-link dispatch is not yet qualified.

Actual macOS release +18 captures use the production Worker with a separate local User: [inbox and unread](native-evidence-2026-09-05-slice3/macos-inbox-inbox.png), [failed-Turn notice](native-evidence-2026-09-05-slice3/macos-inbox-failed-notice.png), [empty after acknowledgement](native-evidence-2026-09-05-slice3/macos-inbox-empty.png), [offline](native-evidence-2026-09-05-slice3/macos-inbox-offline.png). The failure is an actual local provider refusal, not a fabricated UI message. The release build succeeds; 264 Flutter tests and a clean analyzer pass, including 200% text in both palettes and reduced motion.

Design review: shared type and neutral grouped cards, pink explicit actions, desktop width and insets, readable chronology, finite motion, skeletons and specific offline recovery. Pixel still reports `mDreamingLockscreen=true`: **awaiting an unlocked phone**. Cross-device OS-alert claims, background push/channels, physical screen-reader and full performance budgets remain unimplemented or unqualified. This milestone adds no native OS alerts and does not claim global OS-notification exactly-once delivery. Matching macOS provisioning remains required for production verified auth. The [seam plan](native-notifications.md) records scope and the constitutional review.

# Native Connections

The User Durable Object owns Connection grants, credentials, lifecycle and reconciliation. Native Connectors projects only labels, service names and lifecycle states from the same manifest-filtered User configuration as Vue. The Connections surface remains the single home for grants; Models retains model provider accounts. Native opens the existing same-User backend browser handoff for catalog search, authorization, reconnect and revocation. No secret-entry form, provider URL or provider SDK is introduced in Dart.

Closing or disconnecting the native page cancels no admitted grant. Returning from the browser or refreshing reads the User projection again. The existing backend records grant intent, callback consumption and uncertain outcomes; this milestone adds no durable record or effect. Its read works across owner eviction and while the Computer is hibernated. Revoked and disabled Package accounts follow the same surface policy as Vue. Invalid sessions and mismatched owners fail closed with a recoverable native state.

Verification seams, already requested in the brief: browser/native gateway reads over one User owner, grant lifecycle and replay at the existing backend boundary, strict shared DTO fixtures, and native UI recovery/large text. Release screenshots and remaining physical gates go into the dated acceptance ledger. This renderer milestone extends the existing Connections parity capability under ADRs 0019, 0039 and 0042. It depends on C1's same-User browser handoff and settings navigation.

## Constitutional review

| Rule family                                       | Evidence and disposition                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Product intent, feature rule and rollout          | Connections renderer parity under the native plan; owner, commands, disconnect, recovery and boundaries are stated above. No native distribution or catalog promotion.                                                                                                                                          |
| Authorities, durable effects and storage          | The existing User owner handles all grants, replay, revocation and reconciliation. The new DTO is a read projection with no new durable record or cancellation command. Paired gateway tests cross owner eviction and repeated callback delivery.                                                               |
| One production path and configuration homes       | Browser and native project the same User lifecycle. Manifest facts select Connections; model accounts remain on Models. There is no Package lifecycle or per-Bot grant control here. The system-browser handoff opens this same trusted home for authorization and account management.                          |
| Minimal kernel, composition and self-modification | No kernel, Package lifecycle, pinned generation, executable extension or Bot authority changes. Required navigation and failure chrome is compiled Flutter code.                                                                                                                                                |
| Integrations, trust and client windows            | The new shared schema carries labels, names and states only; extra credential metadata fails decoding. Dart imports Flutter, generated DTOs, transport and URL launcher. Native/browser handoff tests refuse mismatched Users and arbitrary origins. Existing wire shapes and minimum version remain unchanged. |
| Computer, Workspace and Memory                    | No Computer, durable-root, Memory or provider SDK import is introduced. Grant state and retries remain backend-owned and do not depend on a native process. Full live hibernation qualification stays in the Slice 2 ledger.                                                                                    |
| Native quality and landing                        | Shared theme, safe insets, bounded desktop width, finite reduced-motion-aware account entry, skeletons, clear status and retry copy, and 200% text tests. Required checks and real release captures are recorded in the dated ledger; orchestrator-owned tags remain separate.                                  |

Catalog search, credential entry and grant-management controls currently use the
backend browser surface. The native page gives a clear system-browser handoff
and then displays the resulting durable state. This is the chosen complete
authorization path for this milestone; a separately rendered native catalog is
not claimed. A closed browser or native process does not cancel an admitted grant.

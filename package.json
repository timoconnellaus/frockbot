{
  "name": "frockbot",
  "version": "0.0.1",
  "private": true,
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "applications/*",
    "apps/*",
    "packages/*"
  ],
  "trustedDependencies": [
    "electron"
  ],
  "scripts": {
    "build:cordis-agent": "bun run --filter @frockbot/agent-runtime build",
    "build:webui": "bun run --filter @frockbot/plugin-ui-theme build && bun run --filter @frockbot/plugin-shell build && bun run --filter @frockbot/plugin-settings build && bun run --filter @frockbot/plugin-custom-models build && bun run --filter @frockbot/plugin-routines build && bun run --filter @frockbot/plugin-bot-template build && bun run --filter @frockbot/plugin-package-publisher build && bun run --filter @frockbot/plugin-flock build && bun run --filter @frockbot/plugin-computer build",
    "build:agents": "bun run build:cordis-agent",
    "build:cloudflare": "bun run --filter @frockbot/cloudflare build",
    "build:marketing": "bun run --filter @frockbot/marketing build",
    "build:mobile": "bun run build:webui && bun run --filter @frockbot/mobile build",
    "icons:generate": "./scripts/generate-app-icons.sh",
    "dev:cloudflare": "bun run --filter @frockbot/cloudflare dev",
    "dev:cloudflare:electron": "bun run --filter @frockbot/cloudflare dev:electron",
    "dev": "bun run build:webui && bun run --filter @frockbot/desktop dev",
    "build": "bun run build:webui && bun run --filter @frockbot/desktop build && bun run build:cloudflare && bun run build:marketing",
    "proof:cordis": "bun run --filter @frockbot/cordis-poc typecheck && bun run --filter @frockbot/cordis-poc test && bun run --filter @frockbot/agent-runtime build && bun run --filter @frockbot/cordis-poc smoke",
    "typecheck": "bun scripts/check-kernel-imports.ts && bun scripts/check-computer-host-imports.ts && bun scripts/generate-isolate-context-catalog.ts --check && bun run --filter '*' typecheck",
    "lint:ui-styles": "bun scripts/check-ui-styles.ts",
    "lint:kernel-imports": "bun scripts/check-kernel-imports.ts",
    "lint:computer-host-imports": "bun scripts/check-computer-host-imports.ts",
    "test": "bun test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint-staged": "lint-staged",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "@types/css-tree": "2.3.11",
    "@vue/compiler-sfc": "3.5.41",
    "css-tree": "3.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "prettier": "^3.9.6",
    "prettier-plugin-sh": "^0.19.0",
    "typescript": "5.9.3"
  }
}

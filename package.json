{
  "name": "frockbot",
  "version": "0.0.1",
  "private": true,
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "applets",
    "applets/sdk",
    "applications/*",
    "apps/*",
    "computer",
    "core",
    "packages/*",
    "providers"
  ],
  "overrides": {
    "element-plus": "2.14.5",
    "file-type": "^21.3.1",
    "uuid": "^11.1.1"
  },
  "scripts": {
    "build:webui": "bun run --filter @frockbot/plugin-ui-theme build && bun run --filter @frockbot/plugin-shell build && bun run --filter @frockbot/plugin-settings build && bun run --filter @frockbot/plugin-custom-models build && bun run --filter @frockbot/plugin-routines build && bun run --filter @frockbot/plugin-bot-template build && bun run --filter @frockbot/plugin-flock build && bun run --filter @frockbot/computer build",
    "build:cloudflare": "bun scripts/build-applets-assets.ts && bun run --filter @frockbot/cloudflare build",
    "build:marketing": "bun run --filter @frockbot/marketing build",
    "bootstrap:npm-trust": "./scripts/bootstrap-npm-trust.sh",
    "dogfood:dev": "./scripts/dogfood/dev-stack.sh",
    "dev:native": "bun scripts/native-dev.ts up",
    "smoke:native": "bun scripts/native-dev.ts smoke",
    "dogfood:stop": "./scripts/dogfood/dev-stack.sh stop",
    "dev:cloudflare": "bun run --filter @frockbot/cloudflare dev",
    "dev": "bun run build:webui && bun run dev:cloudflare",
    "build": "bun run build:webui && bun run build:cloudflare && bun run build:marketing",
    "typecheck": "bun scripts/check-client-protocol.ts && bun scripts/check-layer-imports.ts && bun scripts/check-computer-host-imports.ts && bun scripts/generate-isolate-context-catalog.ts --check && bun scripts/build-applets-assets.ts --check && bun scripts/typecheck.ts",
    "lint:ui-styles": "bun scripts/check-ui-styles.ts",
    "lint:layer-imports": "bun scripts/check-layer-imports.ts",
    "lint:computer-host-imports": "bun scripts/check-computer-host-imports.ts",
    "test": "bun test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint-staged": "lint-staged",
    "prepare": "husky",
    "generate:protocol": "bun scripts/generate-typescript-protocol.ts && bun scripts/generate-dart-protocol.ts"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "@types/css-tree": "2.3.11",
    "@vue/compiler-sfc": "3.5.41",
    "css-tree": "3.2.1",
    "esbuild": "0.28.2",
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "playwright": "1.62.1",
    "prettier": "^3.9.6",
    "prettier-plugin-sh": "^0.19.0",
    "typescript": "npm:typescript-native-bridge@6.0.3-bridge.16.tsgo.7.0.2",
    "ajv": "8.18.0"
  }
}

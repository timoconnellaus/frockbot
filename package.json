{
  "name": "frockbot",
  "version": "0.0.1",
  "private": true,
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "applications/*",
    "apps/*",
    "packages/*"
  ],
  "trustedDependencies": [
    "electron"
  ],
  "scripts": {
    "build:cordis-agent": "bun run --filter @frockbot/agent-runtime build",
    "build:webui": "bun run --filter @frockbot/plugin-shell build && bun run --filter @frockbot/plugin-clock build && bun run --filter @frockbot/plugin-computer build",
    "build:agents": "bun run build:cordis-agent",
    "build:cloudflare": "bun run --filter @frockbot/cloudflare build",
    "build:marketing": "bun run --filter @frockbot/marketing build",
    "build:mobile": "bun run build:webui && bun run --filter @frockbot/mobile build",
    "icons:generate": "./scripts/generate-app-icons.sh",
    "dev:cloudflare": "bun run --filter @frockbot/cloudflare dev",
    "dev:cloudflare:electron": "bun run --filter @frockbot/cloudflare dev:electron",
    "dev": "bun run build:webui && bun run build:agents && bun run --filter @frockbot/desktop dev",
    "build": "bun run build:webui && bun run build:agents && bun run --filter @frockbot/desktop build && bun run build:cloudflare && bun run build:marketing",
    "proof:cordis": "bun run --filter @frockbot/cordis-poc typecheck && bun run --filter @frockbot/cordis-poc test && bun run --filter @frockbot/agent-runtime build && bun run --filter @frockbot/cordis-poc smoke",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint-staged": "lint-staged",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "prettier": "^3.9.6",
    "prettier-plugin-sh": "^0.19.0"
  }
}

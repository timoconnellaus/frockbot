Merge origin/main: take 165's shared unconfigured-Computer message

PR 165 centralised the no-Computer copy into
COMPUTER_UNCONFIGURED_MESSAGE_V1 and put the Bot's name in the viewer
header — the same two fixes by a better route, so its wording and its
header win over mine. The one thing kept from this branch is dropping
the vendor from the configured message: "Persistent Fly Sprite computer"
became "Computer ready".

Also plainifies two projection messages the merge surfaced: "Reconnect
to mint a fresh viewer session" and "Persistent Computer available".

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>

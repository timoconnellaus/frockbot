# The Applets SDK pins `typescript` to the tsgo bridge, whose version is a
# prerelease. A semver range without one — `@tanstack/db`, peer `typescript
# >=4.7` — does not match a prerelease, so npm refuses a tree bun installs
# without comment. The peer is satisfied in fact; only the range says otherwise.
legacy-peer-deps=true

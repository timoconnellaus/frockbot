{
  "id": "credentials",
  "displayName": "Credential cards",
  "version": "1",
  "contractVersion": 5,
  "tools": [],
  "hooks": [],
  "grants": [],
  "cards": [
    {
      "id": "request",
      "displayName": "Credential request",
      "description": "Say what credential is missing and where the person adds it. The value never crosses the conversation.",
      "dataSchema": {
        "type": "object",
        "properties": {
          "prompt": { "type": "string", "minLength": 1, "maxLength": 2000 },
          "secretName": { "type": "string", "minLength": 1, "maxLength": 128 }
        },
        "required": ["prompt", "secretName"],
        "additionalProperties": false
      },
      "actions": []
    }
  ],
  "contextKeys": ["user", "bot", "session"]
}

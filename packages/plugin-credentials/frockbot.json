{
  "schemaVersion": 3,
  "id": "credentials",
  "displayName": "Saved credentials",
  "version": "0.0.1",
  "compatibility": { "frockbot": ">=0.0.1" },
  "contributions": {
    "backend": [{ "entry": "./user", "host": "user" }],
    "runtime": { "entry": "./runtime" }
  },
  "permissions": ["credentials:manage"]
}
